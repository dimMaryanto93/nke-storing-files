apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-springboot3-logging
spec:
  storageClassName: csi-s3
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Gi
  volumeMode: Filesystem
